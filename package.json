{
  "name": "sample-plugin",
  "version": "0.1.0",
  "description": "A sample plugin for Xola App store",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "hapi": "^16.0.2"
  }
}
